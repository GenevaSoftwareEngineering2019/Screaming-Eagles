function init(self)
	msg.post("#music", "stop_sound")
end

function on_message(self, message_id, message, sender)
	if message_id == hash("Freeze") then
		msg.post("Gamescreenproxy1", "set_time_step", {factor = 0.0001, mode = 1})
	end
end
