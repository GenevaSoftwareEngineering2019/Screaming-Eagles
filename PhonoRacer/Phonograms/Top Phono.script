function init(self)
	self.t = 2 
	local s = vmath.vector3(0.3, 0.3, 0.3)
	go.set_scale(s)
end

function update(self, dt)
	
	local speed = -60

	local p = go.get_position()
	p.x = p.x + speed * dt
	if p.x <= 800 then
		p.x = 800
	end
	go.set_position(p)
end